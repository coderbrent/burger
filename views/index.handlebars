<h1>Burgers I'd Like To Consume</h1>

<h2>Burgers That I've Voraciously Devoured</h2>

<ul>
  {{#each burgers}}
  <li>{{burger}}</li>
    {{!-- {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}} --}}
  {{/each}}
</ul>

<h2>Burgers I've Not Yet Eaten</h2>

<ul>
  {{#each burgers}}
    {{!-- {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}} --}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Eaten?</label><br>
    <input type="radio" name="eaten" value="1"> Eaten!<br>
    <input type="radio" name="not-eaten" value="0"> Not Eaten!
  </div>

  <button type="submit">Add Burger</button>
</form>
