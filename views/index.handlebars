<h1>Burgers I'd Like To Consume</h1>

<h3>Burgers That I've EATEN</h3>

<ul>
  {{#each burgers}}
  <li>Burger: {{burger_name}}</li>
    {{#if devoured}}
      {{> burgers/burgers-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h3>Burgers I've Not Yet EATEN</h3>

<ul>
  {{#each burgers}}
      {{burgers}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Eaten?</label><br>
    <input type="radio" name="eaten" value="1"> Eaten!<br>
    <input type="radio" name="not-eaten" value="0"> Not Eaten!
  </div>

  <button type="submit">Add Burger</button>
</form>
